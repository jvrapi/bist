import { StyleSheet } from 'react-native'
export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    borderColor: 'green',
    borderWidth: 3,
    borderRadius: 70,
    textAlign: 'center',
    justifyContent: 'center',
    height: 80,
    width: 80
  },
  button: {
    height: 100,
    width: 100,
    alignItems: 'center',
    justifyContent: 'center',
    flex: 1
  }
})
